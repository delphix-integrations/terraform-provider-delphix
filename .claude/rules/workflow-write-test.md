# Write Test Workflow

When the user asks to write, add, generate, or create tests for a resource — or says things like "write tests for X", "add test coverage for delphix_X", "I need tests for the X resource" — automatically invoke the `/write-test` skill with the resource name as the argument. Do NOT wait for the user to type `/write-test` explicitly.
